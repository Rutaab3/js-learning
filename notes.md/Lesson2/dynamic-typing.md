> <h1> <center>DYNAMIC TYPING


`In other languages we have static typing which means we declare a varibale with its type`


> <h2> for e.g in java

```java
 int a = 12
 string b = "Rutaab"
 boolean c = true
```


`Javascript has dynamic typing which means we can create a variable and can intialize it multiple times but with different datatypes`


```js
let a = "Rutaab"          //string
a = 99                    //number
a = true                  //boolean
a = undefined             //undefined
a = null                  //object â† known bug
a = [ ]                   //object
a = { }                   //object
a = function( ){ }        //function
```


> <h3> Q. Which typing is best?

`A. Static typing is the known winner as dynamic typing will cause error that are difficult ta takle specially if you have a massive code base.`
